coverage:
  status:
    project:
      default:
        target: 55%
        threshold: 2%
    patch:
      default:
        target: 90%

comment:
  layout: "header, diff, files"
  behavior: default
  require_changes: false
